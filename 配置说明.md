# 死锁问题解决方案配置说明

## 1. 代码集成步骤

### 步骤1：将文件添加到项目
- `IndexCustomSqlMapper.java` → `src/main/java/org/springblade/modules/index/mapper/`
- `IndexCustomSqlMapper.xml` → `src/main/resources/mapper/index/`
- `IndexCustomSqlService.java` → `src/main/java/org/springblade/modules/index/service/`

### 步骤2：修改现有调用代码
将原来的直接调用Mapper改为调用Service：

**修改前：**
```java
@Autowired
private IndexCustomSqlMapper indexCustomSqlMapper;

public void deleteData() {
    indexCustomSqlMapper.updateCustomSql(indexId, indexParams, 300);
}
```

**修改后：**
```java
@Autowired
private IndexCustomSqlService indexCustomSqlService;

public void deleteData() {
    indexCustomSqlService.updateCustomSqlWithRetry(indexId, indexParams, 300);
}
```

## 2. 数据库优化步骤

### 步骤1：执行索引优化SQL
运行 `数据库优化SQL.sql` 中的SQL语句，创建必要的索引。

### 步骤2：检查索引是否生效
```sql
EXPLAIN DELETE FROM ks_index_user_performance_details 
WHERE index_id = 349 AND index_params = '2025-11@202502258001' 
LIMIT 300;
```

确保查询使用了索引（key列不为NULL）。

## 3. 配置调整

### Spring事务配置（可选）
如果需要在application.yml中配置事务超时：

```yaml
spring:
  datasource:
    hikari:
      # 连接超时设置
      connection-timeout: 30000
      # 事务超时（通过编程式事务控制）
```

### MyBatis配置（可选）
确保MyBatis配置正确：

```yaml
mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: org.springblade.modules
```

## 4. 监控和诊断

### 查看死锁日志
```sql
SHOW ENGINE INNODB STATUS;
```

在输出中查找 `LATEST DETECTED DEADLOCK` 部分。

### 启用死锁日志记录
在MySQL配置文件中添加：
```ini
innodb_print_all_deadlocks = ON
```

## 5. 最佳实践

1. **使用分批删除**：对于大数据量，使用 `updateCustomSqlInBatches` 方法
2. **合理设置批次大小**：根据数据量和并发情况调整批次大小（建议50-200）
3. **监控重试次数**：如果频繁重试，考虑优化索引或调整业务逻辑
4. **缩短事务时间**：避免在事务中进行耗时操作
5. **统一锁顺序**：确保多个事务访问相同资源时，锁的顺序一致

## 6. 测试建议

1. 在测试环境先执行索引优化SQL
2. 测试死锁重试机制是否正常工作
3. 模拟并发场景，验证解决方案有效性
4. 监控生产环境死锁发生频率
