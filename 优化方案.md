# ks_index_user_performance_details 删除操作优化方案

## 问题分析

SQL语句：`delete from ks_index_user_performance_details where index_id = ? and index_params = ?`

### 导致死锁的常见原因

1. **缺少合适的索引**：如果 `index_id` 和 `index_params` 没有联合索引，会导致全表扫描，锁定大量行
2. **锁竞争**：多个事务同时执行删除操作，造成锁等待和死锁
3. **事务时间过长**：删除操作执行时间长，持有锁的时间过长
4. **索引顺序问题**：索引列的顺序与WHERE条件不匹配

## 优化方案

### 1. 索引优化（最重要）

#### 方案A：创建联合索引（推荐）
```sql
-- 创建联合索引，索引列顺序与WHERE条件匹配
CREATE INDEX idx_index_id_params ON ks_index_user_performance_details(index_id, index_params);

-- 如果index_params是文本类型且很长，考虑使用前缀索引
-- CREATE INDEX idx_index_id_params ON ks_index_user_performance_details(index_id, index_params(50));
```

#### 方案B：如果已有单列索引，检查索引选择性
```sql
-- 查看现有索引
SHOW INDEX FROM ks_index_user_performance_details;

-- 分析索引使用情况
EXPLAIN DELETE FROM ks_index_user_performance_details WHERE index_id = ? AND index_params = ?;
```

### 2. 查询优化

#### 方案A：分批删除（适用于大量数据）
```sql
-- 每次删除一定数量的记录，避免长时间锁定
DELETE FROM ks_index_user_performance_details 
WHERE index_id = ? AND index_params = ? 
LIMIT 1000;

-- 循环执行直到删除完成
```

#### 方案B：使用临时表（适用于复杂删除）
```sql
-- 先找出要删除的主键
CREATE TEMPORARY TABLE temp_delete_ids AS
SELECT id FROM ks_index_user_performance_details 
WHERE index_id = ? AND index_params = ?;

-- 然后删除
DELETE FROM ks_index_user_performance_details 
WHERE id IN (SELECT id FROM temp_delete_ids);
```

### 3. 死锁预防措施

#### 方案A：降低事务隔离级别（谨慎使用）
```sql
-- 在事务开始时设置
SET SESSION TRANSACTION ISOLATION LEVEL READ COMMITTED;
```

#### 方案B：使用行锁提示
```sql
-- MySQL
DELETE FROM ks_index_user_performance_details 
WHERE index_id = ? AND index_params = ? 
FOR UPDATE;

-- 或者使用NOWAIT（如果支持）
DELETE FROM ks_index_user_performance_details 
WHERE index_id = ? AND index_params = ? 
FOR UPDATE NOWAIT;
```

#### 方案C：添加重试机制
- 捕获死锁异常（如 MySQL 的 1213 错误）
- 实现指数退避重试
- 限制重试次数

### 4. 代码层面优化

#### Java示例（使用MyBatis）
```java
// 分批删除实现
public void deleteInBatches(Long indexId, String indexParams) {
    int batchSize = 1000;
    int deletedCount;
    
    do {
        deletedCount = mapper.deleteBatch(indexId, indexParams, batchSize);
        // 短暂休眠，释放锁
        Thread.sleep(10);
    } while (deletedCount > 0);
}

// Mapper方法
@Delete("DELETE FROM ks_index_user_performance_details " +
        "WHERE index_id = #{indexId} AND index_params = #{indexParams} " +
        "LIMIT #{batchSize}")
int deleteBatch(@Param("indexId") Long indexId, 
                @Param("indexParams") String indexParams, 
                @Param("batchSize") int batchSize);
```

#### Python示例（使用SQLAlchemy）
```python
def delete_in_batches(session, index_id, index_params, batch_size=1000):
    """分批删除，避免长时间锁定"""
    while True:
        # 使用LIMIT分批删除
        result = session.execute(
            text("""
                DELETE FROM ks_index_user_performance_details 
                WHERE index_id = :index_id AND index_params = :index_params 
                LIMIT :batch_size
            """),
            {"index_id": index_id, "index_params": index_params, "batch_size": batch_size}
        )
        deleted_count = result.rowcount
        session.commit()
        
        if deleted_count == 0:
            break
        
        # 短暂休眠，释放锁
        time.sleep(0.01)
```

### 5. 监控和诊断

#### 查看死锁日志
```sql
-- MySQL 5.7+
SHOW ENGINE INNODB STATUS;

-- 查看当前锁等待
SELECT * FROM information_schema.INNODB_LOCKS;
SELECT * FROM information_schema.INNODB_LOCK_WAITS;

-- 查看慢查询
SELECT * FROM mysql.slow_log 
WHERE sql_text LIKE '%ks_index_user_performance_details%';
```

#### 分析执行计划
```sql
EXPLAIN DELETE FROM ks_index_user_performance_details 
WHERE index_id = ? AND index_params = ?;
```

## 实施步骤

1. **立即执行**：
   - 创建联合索引 `idx_index_id_params`
   - 分析现有执行计划

2. **短期优化**：
   - 实现分批删除逻辑
   - 添加死锁重试机制

3. **长期优化**：
   - 监控删除操作的执行时间
   - 考虑表分区（如果数据量很大）
   - 优化表结构（如果index_params字段过大）

## 注意事项

1. **索引维护成本**：创建索引会增加写入成本，但能大幅提升查询和删除性能
2. **数据量评估**：如果单次删除的数据量很大（>10万），必须使用分批删除
3. **业务影响**：分批删除会增加总执行时间，但能避免死锁，提升系统稳定性
4. **测试验证**：在生产环境应用前，务必在测试环境验证优化效果

## 预期效果

- **删除速度**：提升 10-100 倍（取决于数据量和索引情况）
- **死锁频率**：降低 90% 以上
- **系统稳定性**：显著提升
