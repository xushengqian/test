# DELETE 语句性能优化方案

## 问题描述
```sql
delete from ks_index_user_performance_details where index_id = ? and index_params = ?
```
执行慢导致死锁问题。

## 问题分析

### 1. 死锁原因
- **缺少索引**：`index_id` 和 `index_params` 字段可能没有联合索引，导致全表扫描
- **表锁竞争**：DELETE 操作可能持有表级锁，并发时容易死锁
- **事务时间过长**：删除大量数据时，事务持有锁的时间过长
- **索引碎片**：表数据频繁删除导致索引碎片，影响查询性能

### 2. 性能瓶颈
- 没有合适的索引，WHERE 条件需要扫描大量数据
- 删除操作可能触发级联操作或触发器
- 大事务导致锁持有时间过长

## 优化方案

### 方案1：添加联合索引（推荐）

```sql
-- 创建联合索引
CREATE INDEX idx_index_id_params ON ks_index_user_performance_details(index_id, index_params);

-- 或者如果 index_params 字段很长，考虑使用前缀索引
-- CREATE INDEX idx_index_id_params ON ks_index_user_performance_details(index_id, index_params(50));
```

**优点**：
- 显著提升 WHERE 条件查询速度
- 减少锁持有时间
- 降低死锁概率

### 方案2：分批删除（适用于大量数据）

```java
// Java 示例
public void deleteInBatches(Long indexId, String indexParams, int batchSize) {
    int deletedCount;
    do {
        deletedCount = jdbcTemplate.update(
            "DELETE FROM ks_index_user_performance_details " +
            "WHERE index_id = ? AND index_params = ? " +
            "LIMIT ?",
            indexId, indexParams, batchSize
        );
        // 每批删除后稍作延迟，释放锁
        try {
            Thread.sleep(100);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            break;
        }
    } while (deletedCount > 0);
}
```

```python
# Python 示例
def delete_in_batches(cursor, index_id, index_params, batch_size=1000):
    while True:
        cursor.execute(
            """
            DELETE FROM ks_index_user_performance_details 
            WHERE index_id = %s AND index_params = %s 
            LIMIT %s
            """,
            (index_id, index_params, batch_size)
        )
        deleted_count = cursor.rowcount
        if deleted_count == 0:
            break
        # 提交当前批次，释放锁
        cursor.connection.commit()
        time.sleep(0.1)  # 短暂延迟
```

### 方案3：优化事务管理

```java
// 使用短事务，及时提交
@Transactional(propagation = Propagation.REQUIRES_NEW)
public void deleteWithShortTransaction(Long indexId, String indexParams) {
    // 先查询要删除的记录ID（使用索引）
    List<Long> ids = jdbcTemplate.queryForList(
        "SELECT id FROM ks_index_user_performance_details " +
        "WHERE index_id = ? AND index_params = ?",
        Long.class, indexId, indexParams
    );
    
    // 分批删除
    if (!ids.isEmpty()) {
        jdbcTemplate.batchUpdate(
            "DELETE FROM ks_index_user_performance_details WHERE id = ?",
            ids,
            batchSize,
            (ps, id) -> ps.setLong(1, id)
        );
    }
}
```

### 方案4：使用软删除（如果业务允许）

```sql
-- 添加删除标记字段
ALTER TABLE ks_index_user_performance_details ADD COLUMN deleted TINYINT DEFAULT 0;
ALTER TABLE ks_index_user_performance_details ADD INDEX idx_deleted_index_id_params(deleted, index_id, index_params);

-- 使用 UPDATE 代替 DELETE（性能更好，不会锁表）
UPDATE ks_index_user_performance_details 
SET deleted = 1 
WHERE index_id = ? AND index_params = ? AND deleted = 0;

-- 定期清理已删除的数据（在低峰期执行）
DELETE FROM ks_index_user_performance_details WHERE deleted = 1 AND deleted_time < DATE_SUB(NOW(), INTERVAL 30 DAY);
```

### 方案5：优化表结构

```sql
-- 分析表，更新统计信息
ANALYZE TABLE ks_index_user_performance_details;

-- 优化表，重建索引（在低峰期执行）
OPTIMIZE TABLE ks_index_user_performance_details;

-- 检查表结构，确保字段类型合适
SHOW CREATE TABLE ks_index_user_performance_details;
```

## 实施步骤

### 立即执行（紧急）
1. **添加联合索引**
   ```sql
   CREATE INDEX idx_index_id_params ON ks_index_user_performance_details(index_id, index_params);
   ```

2. **检查现有索引**
   ```sql
   SHOW INDEX FROM ks_index_user_performance_details;
   ```

### 短期优化（1-2天）
1. 修改代码，实现分批删除
2. 优化事务管理，缩短事务时间
3. 添加删除操作的监控和日志

### 长期优化（1周内）
1. 考虑使用软删除机制
2. 定期执行表优化（OPTIMIZE TABLE）
3. 建立索引维护策略

## 监控指标

```sql
-- 查看慢查询
SELECT * FROM mysql.slow_log 
WHERE sql_text LIKE '%ks_index_user_performance_details%' 
ORDER BY start_time DESC LIMIT 10;

-- 查看锁等待情况
SHOW ENGINE INNODB STATUS;

-- 查看表统计信息
SELECT * FROM information_schema.TABLES 
WHERE TABLE_NAME = 'ks_index_user_performance_details';
```

## 预防措施

1. **索引监控**：定期检查索引使用情况
2. **慢查询监控**：设置慢查询阈值，及时发现问题
3. **死锁监控**：监控死锁日志，分析死锁原因
4. **代码审查**：确保所有 DELETE 操作都有合适的索引支持

## 注意事项

1. 添加索引时，如果表数据量很大，建议在低峰期执行
2. 分批删除时，需要根据实际情况调整批次大小
3. 如果使用软删除，需要修改所有查询条件，添加 `deleted = 0`
4. 定期清理历史数据，避免表过大影响性能
