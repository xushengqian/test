# DateTimeParseException 修复说明

## 错误原因

当使用 `LocalDateTime.parse()` 解析只包含日期（没有时间）的字符串时会出现此错误。

**错误示例：**
```java
String dateString = "2024-07-01";
LocalDateTime dateTime = LocalDateTime.parse(dateString); // ❌ 抛出异常
```

## 解决方案

### 方案 1: 使用 LocalDate.parse()（推荐）

如果只需要日期信息，使用 `LocalDate`：

```java
String dateString = "2024-07-01";
LocalDate date = LocalDate.parse(dateString); // ✅
```

### 方案 2: 转换为 LocalDateTime

如果需要 `LocalDateTime`，可以在解析后添加默认时间：

```java
String dateString = "2024-07-01";
LocalDateTime dateTime = LocalDate.parse(dateString).atStartOfDay(); // ✅ 默认 00:00:00
```

### 方案 3: 使用 DateTimeFormatter

使用自定义格式化器：

```java
String dateString = "2024-07-01";
DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd");
LocalDate date = LocalDate.parse(dateString, formatter);
LocalDateTime dateTime = date.atStartOfDay();
```

### 方案 4: 确保字符串包含时间部分

如果确实需要解析为 `LocalDateTime`，确保字符串包含时间：

```java
String dateTimeString = "2024-07-01T10:30:00"; // 注意 T 分隔符
LocalDateTime dateTime = LocalDateTime.parse(dateTimeString); // ✅
```

## 关键区别

- **LocalDate**: 只包含日期（年-月-日）
- **LocalDateTime**: 包含日期和时间（年-月-日T时:分:秒）

## 常见格式

- `LocalDate`: `"2024-07-01"` (ISO-8601 日期格式)
- `LocalDateTime`: `"2024-07-01T10:30:00"` (ISO-8601 日期时间格式)
