# 分批删除实现说明

## 原始代码问题

1. **一次性删除所有记录**：如果数据量大，可能导致：
   - 数据库锁定时间过长
   - 事务日志过大
   - 影响其他操作性能

2. **SQL注入风险**：直接拼接SQL字符串，存在安全隐患

## 改进方案

### 方案1：使用 DELETE ... LIMIT（推荐，适用于MySQL）

```java
// 每批删除的记录数
private static final int BATCH_SIZE = 1000;

String originSqlCount = "SELECT COUNT(*) AS count FROM " + table + 
                       " WHERE index_id = " + indexId + 
                       " AND index_params = '" + indexParams + "'";
int count = getCountVal(originSqlCount);

if (count == 0) {
    return;
}

// 分批删除
int deletedTotal = 0;
while (deletedTotal < count) {
    String sql = "DELETE FROM " + table + 
                " WHERE index_id = " + indexId + 
                " AND index_params = '" + indexParams + "'" +
                " LIMIT " + BATCH_SIZE;
    
    int deletedRows = customSqlExecuteService.executeCustomSqlUpdate(sql);
    deletedTotal += deletedRows;
    
    if (deletedRows == 0) {
        break;
    }
}
```

### 方案2：使用子查询（适用于PostgreSQL等不支持DELETE LIMIT的数据库）

```java
String sql = "DELETE FROM " + table + 
            " WHERE index_id = " + indexId + 
            " AND index_params = '" + indexParams + "'" +
            " AND id IN (" +
            "   SELECT id FROM (" +
            "     SELECT id FROM " + table + 
            "     WHERE index_id = " + indexId + 
            "     AND index_params = '" + indexParams + "'" +
            "     LIMIT " + BATCH_SIZE +
            "   ) AS temp" +
            ")";
```

## 改进点

1. ✅ **分批删除**：每次只删除 BATCH_SIZE 条记录，避免一次性删除大量数据
2. ✅ **循环控制**：通过 deletedTotal 跟踪已删除数量，确保所有记录都被删除
3. ✅ **提前退出**：如果某次删除返回0，说明已删除完毕，提前退出循环
4. ⚠️ **SQL注入**：建议进一步使用参数化查询（PreparedStatement）

## 进一步优化建议

### 1. 使用参数化查询（防止SQL注入）

```java
String sql = "DELETE FROM " + table + 
            " WHERE index_id = ? AND index_params = ? LIMIT ?";
PreparedStatement ps = connection.prepareStatement(sql);
ps.setInt(1, indexId);
ps.setString(2, indexParams);
ps.setInt(3, BATCH_SIZE);
ps.executeUpdate();
```

### 2. 添加事务控制

```java
try {
    connection.setAutoCommit(false);
    // 分批删除逻辑
    connection.commit();
} catch (Exception e) {
    connection.rollback();
    throw e;
}
```

### 3. 添加日志和监控

```java
log.info("开始分批删除，总数: {}, 批次大小: {}", count, BATCH_SIZE);
// ... 删除逻辑 ...
log.info("批次 {} 完成，已删除 {} 条", batchNumber, deletedTotal);
```

### 4. 添加延迟控制（可选）

```java
if (batchNumber > 0 && batchNumber % 10 == 0) {
    Thread.sleep(100); // 每10批暂停100ms，减轻数据库压力
}
```

## 批次大小建议

- **小数据量（< 10万）**：BATCH_SIZE = 1000
- **中等数据量（10万-100万）**：BATCH_SIZE = 5000
- **大数据量（> 100万）**：BATCH_SIZE = 10000

根据实际情况调整，平衡删除速度和数据库压力。
