# MySQL死锁问题解决方案

## 问题分析

根据错误日志，死锁发生在执行以下DELETE语句时：
```sql
DELETE FROM ks_index_user_performance_details 
WHERE index_id = 349 AND index_params = '2025-11@202502258001' 
LIMIT 300
```

## 死锁原因

1. **并发事务冲突**：多个事务同时操作同一张表
2. **锁顺序不一致**：不同事务获取锁的顺序不同
3. **LIMIT子句**：DELETE ... LIMIT 可能导致行级锁竞争
4. **索引缺失**：WHERE条件字段可能缺少合适的索引

## 解决方案

### 方案1：添加死锁重试机制（推荐）

在Service层添加重试逻辑，自动处理死锁异常。

### 方案2：优化SQL语句

- 确保WHERE条件字段有合适的索引
- 考虑分批删除，减少锁持有时间
- 使用更精确的WHERE条件

### 方案3：优化事务管理

- 缩短事务执行时间
- 避免在事务中进行耗时操作
- 使用合适的事务隔离级别

### 方案4：数据库层面优化

- 添加合适的索引（index_id, index_params）
- 考虑使用分区表
- 优化表结构

## 实施步骤

1. 添加死锁重试机制
2. 检查并优化索引
3. 优化SQL语句
4. 调整事务管理策略
