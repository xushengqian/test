# MySQL死锁问题分析与解决方案

## 问题描述

在执行DELETE操作时发生死锁：
```sql
DELETE FROM ks_index_user_performance_details 
WHERE index_id = 349 AND index_params = '2025-11@202502258001' 
LIMIT 300
```

## 死锁原因分析

### 1. 批量删除操作
- 使用 `LIMIT 300` 进行批量删除
- 多个事务同时执行类似操作时，可能以不同顺序获取行锁
- 当两个事务互相等待对方释放锁时，就会发生死锁

### 2. 索引使用问题
- 如果 `index_id` 和 `index_params` 上的索引不够优化
- 可能导致全表扫描或锁范围过大

### 3. 事务范围过大
- 如果事务中包含多个操作，持有锁的时间过长
- 增加了死锁发生的概率

### 4. 并发访问模式
- 多个线程/事务同时操作相同或相关的数据
- 操作顺序不一致导致死锁

## 解决方案

### 方案1：添加重试机制（推荐）
在应用层添加死锁重试逻辑，自动处理死锁异常。

### 方案2：优化SQL和索引
- 确保 `index_id` 和 `index_params` 上有合适的索引
- 考虑使用复合索引 `(index_id, index_params)`

### 方案3：减少批量大小
- 将 `LIMIT 300` 改为更小的值（如 `LIMIT 100`）
- 分批执行，减少单次锁定的行数

### 方案4：调整事务隔离级别
- 如果业务允许，可以考虑使用 `READ COMMITTED` 隔离级别
- 减少锁的持有时间

### 方案5：使用悲观锁或乐观锁
- 根据业务场景选择合适的锁策略
- 避免不必要的锁竞争

### 方案6：优化删除策略
- 考虑使用软删除（标记删除）代替物理删除
- 在低峰期执行批量物理删除

## 最佳实践

1. **始终添加重试机制**：死锁是数据库并发访问的正常现象，应该通过重试来处理
2. **保持事务简短**：减少事务执行时间，降低死锁概率
3. **统一操作顺序**：多个表操作时，保持一致的顺序
4. **监控和日志**：记录死锁信息，分析模式并优化
5. **索引优化**：确保查询条件上的索引是最优的
