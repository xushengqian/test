# 死锁问题解决方案 - 使用说明

## 快速开始

### 1. 立即解决方案（推荐）

在调用 `updateCustomSql` 的地方，使用 `IndexCustomSqlService.updateCustomSqlWithManualRetry()` 方法：

```java
// 原来的代码（会死锁）
indexCustomSqlMapper.updateCustomSql(349L, "2025-11@202502258001", 300);

// 修改为（带重试机制）
indexCustomSqlService.updateCustomSqlWithManualRetry(349L, "2025-11@202502258001", 300);
```

### 2. 长期优化方案

#### 步骤1：优化数据库索引
执行 `数据库优化建议.sql` 中的索引创建语句：
```sql
CREATE INDEX idx_index_id_params 
ON ks_index_user_performance_details(index_id, index_params);
```

#### 步骤2：使用小批量删除
对于大数据量删除，使用批量删除方法：
```java
// 删除300条记录，每次删除100条，自动重试
indexCustomSqlService.deleteInBatches(349L, "2025-11@202502258001", 300);
```

## 方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 手动重试机制 | 无需额外依赖，灵活可控 | 需要手动实现 | **推荐，适用于所有场景** |
| Spring Retry注解 | 代码简洁 | 需要额外依赖 | 如果项目已有spring-retry |
| 小批量删除 | 降低死锁概率 | 需要多次调用 | 大数据量删除 |
| 索引优化 | 根本性解决 | 需要DBA配合 | 必须执行 |

## 代码示例

### 示例1：基本使用（带重试）
```java
@Autowired
private IndexCustomSqlService indexCustomSqlService;

public void deleteData() {
    try {
        int deleted = indexCustomSqlService.updateCustomSqlWithManualRetry(
            349L, 
            "2025-11@202502258001", 
            300
        );
        log.info("成功删除 {} 条记录", deleted);
    } catch (Exception e) {
        log.error("删除失败", e);
    }
}
```

### 示例2：大数据量批量删除
```java
// 删除1000条记录，每次删除100条，自动处理死锁
int totalDeleted = indexCustomSqlService.deleteInBatches(
    349L, 
    "2025-11@202502258001", 
    1000
);
```

### 示例3：使用Spring Retry（如果已添加依赖）
```java
// 在Service中直接使用，自动重试
int deleted = indexCustomSqlService.updateCustomSqlWithRetry(
    349L, 
    "2025-11@202502258001", 
    300
);
```

## 配置说明

### 重试参数调整
在 `IndexCustomSqlService` 中可以调整以下参数：
- `MAX_RETRY_ATTEMPTS`: 最大重试次数（默认3次）
- `RETRY_DELAY_MS`: 初始重试延迟（默认100ms）
- `DEFAULT_BATCH_SIZE`: 默认批次大小（默认100）

### 事务超时设置
如果删除操作可能耗时较长，可以使用带超时的方法：
```java
// 30秒超时
indexCustomSqlService.updateCustomSqlWithTimeout(349L, "2025-11@202502258001", 300);
```

## 监控建议

1. **记录死锁日志**：在Service中已经添加了日志，可以监控死锁频率
2. **监控重试次数**：如果重试次数频繁，说明并发冲突严重，需要优化
3. **分析死锁模式**：定期查看MySQL的 `SHOW ENGINE INNODB STATUS` 输出

## 注意事项

1. ⚠️ **必须执行索引优化**：创建复合索引是根本解决方案
2. ⚠️ **保持事务简短**：不要在事务中执行其他耗时操作
3. ⚠️ **批量大小**：建议将 `LIMIT 300` 改为 `LIMIT 100` 或更小
4. ⚠️ **业务逻辑**：确保重试不会导致业务逻辑问题（如重复删除）

## 故障排查

如果仍然出现死锁：

1. **检查索引**：确认索引已创建且被使用
   ```sql
   EXPLAIN DELETE FROM ks_index_user_performance_details 
   WHERE index_id = 349 AND index_params = '2025-11@202502258001' LIMIT 300;
   ```

2. **查看死锁详情**：
   ```sql
   SHOW ENGINE INNODB STATUS;
   ```

3. **减少并发**：考虑使用队列或锁来控制并发删除操作

4. **调整批次大小**：进一步减小 `LIMIT` 值

## 联系支持

如果问题持续存在，请提供：
- 死锁发生的频率
- 并发访问的线程数
- `SHOW ENGINE INNODB STATUS` 的输出
- 表的数据量和索引情况
