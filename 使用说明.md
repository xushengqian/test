# FreeSWITCH DESTINATION_OUT_OF_ORDER 问题诊断工具包

## 概述

本工具包提供了一套完整的脚本和文档，用于诊断和解决 FreeSWITCH 在高并发场景下出现的 `DESTINATION_OUT_OF_ORDER` 错误。

## 问题背景

- **错误信息**: `-ERR DESTINATION_OUT_OF_ORDER`
- **问题服务器**: 10-181-10-158
- **场景**: 高并发呼叫量时出现
- **表现**: 两台服务器，只有一台出现问题

## 文件清单

### 1. 文档

- **FreeSWITCH_DESTINATION_OUT_OF_ORDER_诊断分析.md** - 详细的问题分析和解决方案文档
  - 错误原因分析
  - 诊断步骤
  - 常见解决方案
  - 预防措施

### 2. 脚本工具

#### freeswitch_monitor.sh - 实时监控脚本
**用途**: 实时监控 FreeSWITCH 的运行状态和关键指标

**功能**:
- 网关状态监控
- 活跃通道统计
- 系统负载监控
- 文件描述符使用率
- 错误日志实时跟踪
- 网络连接统计

**使用方法**:
```bash
# 添加执行权限
chmod +x freeswitch_monitor.sh

# 运行监控（每 10 秒刷新一次）
./freeswitch_monitor.sh

# 后台运行并记录日志
nohup ./freeswitch_monitor.sh > /tmp/monitor.log 2>&1 &
```

**输出**: 实时控制台显示 + 日志文件 `/tmp/freeswitch_monitor.log`

---

#### freeswitch_diagnostic.sh - 全面诊断脚本
**用途**: 生成完整的系统诊断报告

**功能**:
- 系统信息收集
- FreeSWITCH 版本和状态
- 资源限制检查
- 配置文件验证
- 错误日志分析
- 网络配置检查
- 自动生成诊断建议

**使用方法**:
```bash
# 添加执行权限
chmod +x freeswitch_diagnostic.sh

# 运行诊断
./freeswitch_diagnostic.sh

# 诊断报告会保存在
# /tmp/freeswitch_diagnostic_YYYYMMDD_HHMMSS.txt
```

**建议**: 在问题发生时和高峰期各运行一次，对比结果

---

#### compare_servers.sh - 服务器配置对比脚本
**用途**: 对比两台服务器的配置差异，找出问题根源

**功能**:
- FreeSWITCH 版本对比
- 系统资源限制对比
- 关键配置文件对比（MD5 校验）
- 网关状态对比
- 错误率对比
- 自动生成差异报告

**前置条件**:
1. 配置 SSH 免密登录到远程服务器
```bash
# 生成密钥（如果还没有）
ssh-keygen -t rsa

# 复制公钥到远程服务器
ssh-copy-id root@10.181.10.159
```

**使用方法**:
```bash
# 添加执行权限
chmod +x compare_servers.sh

# 对比本地服务器与远程服务器
# 假设本地是 10-181-10-158，远程是另一台正常的服务器
./compare_servers.sh 10.181.10.159

# 对比报告会保存在
# /tmp/freeswitch_comparison_YYYYMMDD_HHMMSS.txt
```

**重点关注**: 配置文件差异和错误率差异

---

#### quick_fix.sh - 快速修复脚本
**用途**: 在问题发生时快速恢复服务

**功能**:
- 交互式菜单操作
- 重启指定网关
- 重启 SIP Profile
- 清理异常通道
- 系统资源优化
- 完全重启 FreeSWITCH（应急）
- 一键全部修复

**使用方法**:
```bash
# 添加执行权限
chmod +x quick_fix.sh

# 运行修复脚本（建议以 root 运行）
sudo ./quick_fix.sh

# 按照菜单提示选择修复操作
```

**修复选项说明**:
1. **重启指定网关** - 最轻量的修复方式，推荐优先尝试
2. **重启 SIP Profile** - 会短暂中断服务，但不影响已有通话
3. **清理异常通道** - 清理卡住的通话通道
4. **检查并优化系统资源** - 检查并尝试优化文件描述符等资源
5. **完全重启 FreeSWITCH** - 最彻底但影响最大，谨慎使用
6. **执行全部修复操作** - 自动执行 1-4 的所有操作

---

## 快速开始指南

### 第一步: 配置权限
```bash
# 进入工具包目录
cd /workspace

# 为所有脚本添加执行权限
chmod +x *.sh
```

### 第二步: 立即诊断
如果问题正在发生，执行以下步骤：

```bash
# 1. 快速修复（优先）
sudo ./quick_fix.sh
# 选择选项 1 (重启网关) 或 6 (全部修复)

# 2. 生成诊断报告
./freeswitch_diagnostic.sh

# 3. 启动实时监控
./freeswitch_monitor.sh
```

### 第三步: 深入分析
```bash
# 1. 对比两台服务器配置
./compare_servers.sh <正常服务器的IP>

# 2. 查看诊断报告
cat /tmp/freeswitch_diagnostic_*.txt

# 3. 查看对比报告
cat /tmp/freeswitch_comparison_*.txt
```

### 第四步: 根据报告实施解决方案
参考 `FreeSWITCH_DESTINATION_OUT_OF_ORDER_诊断分析.md` 中的解决方案

---

## 常见场景处理

### 场景 1: 问题正在发生
```bash
# 立即执行快速修复
sudo ./quick_fix.sh
# 选择: 6 (执行全部修复操作)
```

### 场景 2: 需要找出问题原因
```bash
# 1. 生成诊断报告
./freeswitch_diagnostic.sh

# 2. 对比正常服务器
./compare_servers.sh <正常服务器IP>

# 3. 分析报告中的差异
```

### 场景 3: 持续监控
```bash
# 启动监控脚本
./freeswitch_monitor.sh

# 或后台运行
nohup ./freeswitch_monitor.sh > /tmp/monitor.log 2>&1 &
```

### 场景 4: 高峰期压力测试
```bash
# 在高峰期前、中、后各运行一次诊断
./freeswitch_diagnostic.sh

# 对比不同时间段的报告，找出资源瓶颈
```

---

## 关键检查点

根据经验，以下是高并发场景下最常见的问题：

### 1. 文件描述符限制 ⭐⭐⭐⭐⭐
**检查命令**:
```bash
ulimit -n
lsof -p $(pgrep freeswitch) | wc -l
```

**解决方案**: 增加限制到 65536
```bash
# 编辑 /etc/security/limits.conf
freeswitch soft nofile 65536
freeswitch hard nofile 65536
```

### 2. 并发会话限制 ⭐⭐⭐⭐⭐
**检查位置**: `/etc/freeswitch/autoload_configs/switch.conf.xml`
```xml
<param name="max-sessions" value="2000"/>
<param name="sessions-per-second" value="100"/>
```

### 3. 网关并发限制 ⭐⭐⭐⭐
**现象**: 运营商网关侧有并发限制
**解决方案**: 配置多个网关负载均衡

### 4. RTP 端口耗尽 ⭐⭐⭐
**检查位置**: `/etc/freeswitch/sip_profiles/external.xml`
```xml
<param name="rtp-start-port" value="16384"/>
<param name="rtp-end-port" value="32768"/>
```

### 5. 网络连接问题 ⭐⭐⭐
**检查命令**:
```bash
ping -c 10 <网关IP>
traceroute <网关IP>
```

---

## 监控指标阈值

| 指标 | 正常 | 警告 | 严重 |
|------|------|------|------|
| 文件描述符使用率 | < 60% | 60-80% | > 80% |
| 内存使用率 | < 70% | 70-85% | > 85% |
| CPU 使用率 | < 70% | 70-90% | > 90% |
| 并发通道数 | < 60% max-sessions | 60-80% | > 80% |
| 网关响应时间 | < 50ms | 50-200ms | > 200ms |

---

## 应急联系流程

1. **立即操作**: 运行 `quick_fix.sh` 尝试恢复
2. **收集信息**: 运行 `freeswitch_diagnostic.sh` 生成报告
3. **分析原因**: 对比配置和日志
4. **实施方案**: 根据诊断结果实施相应解决方案
5. **持续监控**: 运行 `freeswitch_monitor.sh` 监控恢复情况

---

## 预防措施

### 日常监控
```bash
# 添加到 crontab，每 5 分钟检查一次
*/5 * * * * /workspace/freeswitch_monitor.sh >> /var/log/fs_monitor.log 2>&1
```

### 自动告警
将监控脚本与告警系统集成，关键指标超阈值时自动告警

### 定期巡检
每周运行一次诊断脚本，对比历史数据，提前发现潜在问题

### 容量规划
根据业务增长趋势，提前扩容或优化配置

---

## 技术支持

如需进一步协助，请提供：
1. 诊断报告 (`freeswitch_diagnostic.sh` 输出)
2. 对比报告 (`compare_servers.sh` 输出)
3. 监控日志 (问题发生时的 `freeswitch_monitor.sh` 日志)
4. 完整的 FreeSWITCH 日志片段
5. 网关供应商的并发规格说明

---

## 版本信息

- **创建日期**: 2025-11-20
- **适用版本**: FreeSWITCH 1.6+
- **测试环境**: CentOS 7/8, Ubuntu 18.04/20.04

---

## 附录: 常用命令速查

```bash
# 查看网关状态
fs_cli -x "sofia status gateway gwopensips"

# 查看通道数
fs_cli -x "show channels count"

# 重启网关
fs_cli -x "sofia profile external killgw gwopensips"
fs_cli -x "sofia profile external rescan"

# 查看 SIP 日志
tail -f /var/log/freeswitch/freeswitch.log | grep -i sip

# 查看资源使用
top -p $(pgrep freeswitch)
lsof -p $(pgrep freeswitch) | wc -l

# 重启 FreeSWITCH
systemctl restart freeswitch
```
