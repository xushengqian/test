# FreeSWITCH Lua 转接判断 - 使用说明

## 📋 项目简介

本项目提供了一套完整的 FreeSWITCH Lua 转接解决方案，用于解决转接人工时如何判断转接是否成功的问题，特别是处理坐席软电话未登录导致的直接挂断问题。

## 🎯 核心功能

### ✅ 主要特性

1. **智能判断**: 转接前检查坐席是否在线，避免无效转接
2. **详细日志**: 记录所有转接尝试和结果
3. **错误处理**: 根据不同失败原因进行不同处理
4. **自动重试**: 支持配置重试次数和间隔
5. **智能路由**: 自动选择可用坐席
6. **数据统计**: 记录转接数据用于分析

### ⚡ 关键优势

- 🚫 **避免直接挂断**: 坐席未登录时不会直接挂断，可以播放提示音或转其他坐席
- 📊 **详细状态信息**: 可以区分未登录、忙线、拒接、无应答等状态
- 🔄 **灵活的重试策略**: 支持自定义重试逻辑
- 📝 **完善的日志记录**: 便于问题排查和数据分析

## 📦 文件说明

| 文件 | 说明 | 用途 |
|------|------|------|
| `transfer_to_agent.lua` | 完整的转接脚本 | ⭐ 生产环境使用 |
| `transfer_simple.lua` | 简化示例 | 📚 学习基本原理 |
| `agent_utils.lua` | 工具函数库 | 🔧 开发自定义功能 |
| `transfer_example.lua` | 使用示例 | 📝 参考实现 |
| `test_agent_status.lua` | 测试脚本 | 🧪 测试和调试 |
| `dialplan_example.xml` | Dialplan配置 | ⚙️ 配置参考 |
| `setup_database.sql` | 数据库脚本 | 💾 初始化数据库 |
| `install.sh` | 安装脚本 | 🚀 快速部署 |
| `README.md` | 详细文档 | 📖 完整说明 |
| `QUICK_REFERENCE.md` | 快速参考 | ⚡ 快速查询 |

## 🚀 快速开始

### 1️⃣ 安装部署

```bash
# 下载项目文件到服务器
cd /workspace

# 运行自动安装脚本（推荐）
sudo ./install.sh

# 或手动安装
sudo cp *.lua /usr/share/freeswitch/scripts/
```

### 2️⃣ 配置 Dialplan

编辑 FreeSWITCH 的 Dialplan 配置：

```xml
<!-- /etc/freeswitch/dialplan/default.xml -->
<extension name="transfer_to_agent">
  <condition field="destination_number" expression="^9000$">
    <!-- 设置要转接的坐席号码 -->
    <action application="set" data="agent_number=1001"/>
    
    <!-- 调用转接脚本 -->
    <action application="lua" data="transfer_to_agent.lua"/>
  </condition>
</extension>
```

### 3️⃣ 重载配置

```bash
fs_cli -x "reloadxml"
```

### 4️⃣ 测试功能

```bash
# 测试坐席状态检查
fs_cli -x "luarun test_agent_status.lua"

# 拨打测试号码（如9000）进行实际测试
```

## 💡 使用示例

### 场景1: 简单转接到单个坐席

**需求**: 将来电转接到1001坐席

**配置**:
```xml
<action application="set" data="agent_number=1001"/>
<action application="lua" data="transfer_to_agent.lua"/>
```

**效果**: 
- ✅ 坐席在线：转接成功
- ❌ 坐席离线：播放"坐席不在线"提示
- 🔄 坐席忙线：可配置是否重试

---

### 场景2: 智能转接到多个坐席

**需求**: 按优先级尝试多个坐席

**使用脚本**: `transfer_example.lua`

**配置**:
```xml
<action application="set" data="transfer_mode=smart"/>
<action application="lua" data="transfer_example.lua"/>
```

**效果**: 自动选择第一个可用的坐席

---

### 场景3: 自定义转接逻辑

**需求**: 根据业务需求定制

**方法**: 使用 `agent_utils.lua` 工具库

```lua
local AgentUtils = require("agent_utils")

-- 检查坐席状态
local status = AgentUtils.getStatus("1001")

if status == "IDLE" then
    -- 坐席空闲，转接
    AgentUtils.transfer(session, "1001", 30)
elseif status == "BUSY" then
    -- 坐席忙线，转其他坐席
    AgentUtils.transfer(session, "1002", 30)
else
    -- 坐席离线，播放提示
    session:streamFile("ivr/ivr-no_user_response.wav")
end
```

## 📊 判断转接结果

### 方法1: 事前检查（推荐）⭐

```lua
local api = freeswitch.API()
local contact = api:executeString("sofia_contact 1001")

if contact and contact ~= "" and not string.find(contact, "error") then
    -- 坐席在线，执行转接
    session:execute("bridge", "user/1001")
else
    -- 坐席离线，不转接
    return false
end
```

### 方法2: 事后判断

```lua
session:execute("bridge", "user/1001")

local cause = session:getVariable("bridge_hangup_cause")

if cause == "SUCCESS" then
    -- 转接成功
elseif cause == "USER_NOT_REGISTERED" then
    -- 坐席未登录
elseif cause == "NO_ANSWER" then
    -- 坐席无应答
end
```

### 方法3: 综合方案（最佳）⭐⭐⭐

```lua
-- 先检查
if not check_registered(agent) then
    return false, "AGENT_OFFLINE"
end

-- 再转接
session:execute("bridge", "user/" .. agent)

-- 后判断
local cause = session:getVariable("bridge_hangup_cause")
return cause == "SUCCESS"
```

## 🔍 关键状态码

| 状态码 | 说明 | 处理建议 |
|--------|------|---------|
| `SUCCESS` | ✅ 转接成功 | 无需处理 |
| `USER_NOT_REGISTERED` | ❌ 坐席未登录 | 播放离线提示，不重试 |
| `NO_ANSWER` | ⏰ 坐席无应答 | 可重试或转其他坐席 |
| `USER_BUSY` | 📞 坐席忙线 | 转其他坐席或提示等待 |
| `CALL_REJECTED` | 🚫 坐席拒接 | 转其他坐席 |

## ⚙️ 配置参数

### 转接参数

```lua
-- 振铃超时（秒）
session:setVariable("call_timeout", "30")

-- 失败后继续执行
session:setVariable("continue_on_fail", "true")

-- 转接后挂断主呼叫
session:setVariable("hangup_after_bridge", "true")
```

### 脚本参数

在 Dialplan 中设置：

```xml
<!-- 坐席号码 -->
<action application="set" data="agent_number=1001"/>

<!-- 最大重试次数 -->
<action application="set" data="max_retry=3"/>

<!-- 转接模式 -->
<action application="set" data="transfer_mode=smart"/>
```

## 🐛 调试技巧

### 1. 查看实时日志

```bash
# 方法1: 使用 tail
tail -f /var/log/freeswitch/freeswitch.log

# 方法2: 使用 fs_cli
fs_cli
> console loglevel 7
```

### 2. 检查坐席状态

```bash
# 检查坐席是否注册
fs_cli -x "sofia_contact 1001"

# 查看注册用户
fs_cli -x "sofia status profile internal reg"

# 查看坐席通话
fs_cli -x "show channels like 1001"
```

### 3. 测试脚本

```bash
# 运行测试脚本
fs_cli -x "luarun test_agent_status.lua"

# 查看变量
# 在Lua脚本中添加：
freeswitch.consoleLog("info", "bridge_hangup_cause: " .. 
    tostring(session:getVariable("bridge_hangup_cause")) .. "\n")
```

## ❓ 常见问题

### Q1: 坐席未登录时为什么直接挂断？

**A**: 默认情况下，bridge失败会挂断呼叫。解决方法：

```lua
session:setVariable("continue_on_fail", "true")
```

### Q2: 如何区分坐席未登录和拒接？

**A**: 检查 `bridge_hangup_cause` 变量：
- `USER_NOT_REGISTERED` = 未登录
- `CALL_REJECTED` = 拒接
- `NO_ANSWER` = 无应答

### Q3: 转接失败后如何处理？

**A**: 可以选择：
1. 播放语音提示
2. 转接到其他坐席
3. 转接到语音信箱
4. 返回IVR菜单

```lua
if not success then
    -- 播放提示
    session:streamFile("ivr/ivr-no_user_response.wav")
    
    -- 或转其他坐席
    transfer_to_agent("1002")
end
```

### Q4: 如何实现轮询转接？

**A**: 使用智能转接功能：

```lua
local AgentUtils = require("agent_utils")
local agent_list = {"1001", "1002", "1003"}
AgentUtils.smartTransfer(session, agent_list, 30)
```

### Q5: 如何记录转接日志？

**A**: 使用数据库记录功能：

```lua
AgentUtils.logToDatabase(
    session:get_uuid(),
    session:getVariable("caller_id_number"),
    agent_number,
    success,
    reason
)
```

## 📈 性能优化

### 1. 复用 API 对象

```lua
-- 不推荐：每次创建新对象
local api1 = freeswitch.API()
local result1 = api1:executeString("sofia_contact 1001")
local api2 = freeswitch.API()
local result2 = api2:executeString("sofia_contact 1002")

-- 推荐：复用对象
local api = freeswitch.API()
local result1 = api:executeString("sofia_contact 1001")
local result2 = api:executeString("sofia_contact 1002")
```

### 2. 缓存坐席状态

对于高并发场景，可以缓存坐席状态，减少API调用。

### 3. 异步处理

对于非关键操作（如日志记录），可以使用异步处理。

## 🔒 安全建议

1. **输入验证**: 验证坐席号码格式
2. **权限控制**: 限制可转接的坐席范围
3. **防止滥用**: 限制转接频率
4. **日志审计**: 记录所有转接操作

## 📚 进阶功能

### 1. 集成呼叫中心系统

```lua
-- 从呼叫中心系统获取坐席状态
local http_client = require("socket.http")
local response = http_client.request("http://callcenter/api/agent/status?id=1001")
```

### 2. 智能排队

根据坐席技能、忙闲状态、等待时间等因素智能分配。

### 3. 统计分析

使用数据库记录的数据进行统计分析：
- 转接成功率
- 平均等待时间
- 坐席工作量

## 📞 技术支持

如遇问题，请：

1. 查看 `README.md` 详细文档
2. 查看 `QUICK_REFERENCE.md` 快速参考
3. 运行 `test_agent_status.lua` 测试环境
4. 检查 FreeSWITCH 日志

## 📝 版本历史

- **v1.0.0** (2025-12-01)
  - 初始版本
  - 支持基本转接判断
  - 支持智能路由
  - 支持数据库日志

## 📄 许可证

本项目代码可自由使用和修改，用于个人或商业项目。

---

**祝您使用愉快！如有问题欢迎反馈。** 🎉
