# 分批删除实现说明

## 概述
将原来的单次删除操作改为分批删除，避免一次性删除大量数据导致数据库锁表或性能问题。

## 实现方案

### 方案1：使用 LIMIT（推荐，适用于 MySQL）
**优点：**
- 实现简单
- 性能较好
- 代码改动小

**代码示例：**
```java
String originSqlCount = "select count(*) as count from " + table 
        + " where index_id = " + indexId 
        + " and index_params = '" + indexParams + "'";
int count = getCountVal(originSqlCount);
if (count == 0) {
    return;
}

int batchSize = 1000;  // 每批删除1000条记录
int deletedCount = 0;
int batchNumber = 0;

while (deletedCount < count) {
    String sql = "delete from " + table 
            + " where index_id = " + indexId 
            + " and index_params = '" + indexParams + "'"
            + " limit " + batchSize;
    
    int affectedRows = customSqlExecuteService.executeCustomSqlUpdate(sql);
    
    if (affectedRows == 0) {
        break;
    }
    
    deletedCount += affectedRows;
    batchNumber++;
    
    // 如果本次删除的记录数小于批次大小，说明已经是最后一批
    if (affectedRows < batchSize) {
        break;
    }
    
    // 可选：添加短暂延迟，避免对数据库造成过大压力
    Thread.sleep(100);
}
```

### 方案2：使用子查询（适用于不支持 DELETE LIMIT 的数据库）
**适用场景：**
- Oracle 数据库
- 其他不支持 DELETE LIMIT 的数据库

**实现思路：**
1. 先查询要删除的主键ID（限制数量）
2. 使用 IN 子句删除这些记录

### 方案3：使用 PreparedStatement（推荐，防止 SQL 注入）
**优点：**
- 防止 SQL 注入攻击
- 更安全

## 关键参数

### batchSize（批次大小）
- **默认值：** 1000
- **建议范围：** 500 - 5000
- **调整原则：**
  - 数据量小：可以设置较大值（如 5000）
  - 数据量大：建议设置较小值（如 500-1000）
  - 根据数据库性能调整

### 延迟时间
- **建议值：** 100-500 毫秒
- **作用：** 避免对数据库造成过大压力
- **可选：** 如果数据库性能足够，可以不添加延迟

## 注意事项

1. **SQL 注入风险：** 当前代码使用字符串拼接，存在 SQL 注入风险。建议使用 PreparedStatement。

2. **数据库兼容性：**
   - MySQL：支持 `DELETE ... LIMIT`
   - PostgreSQL：不支持 `DELETE ... LIMIT`，需要使用方案2
   - Oracle：不支持 `DELETE ... LIMIT`，需要使用方案2
   - SQL Server：不支持 `DELETE ... LIMIT`，需要使用 TOP 关键字

3. **事务处理：** 如果需要在事务中执行，确保整个循环在同一个事务中。

4. **错误处理：** 建议添加异常处理，避免部分删除失败导致数据不一致。

5. **日志记录：** 建议添加日志记录，方便追踪删除进度。

## 改进建议

### 1. 防止 SQL 注入
```java
// 使用 PreparedStatement
String sql = "delete from " + table 
        + " where index_id = ? and index_params = ?"
        + " limit ?";
customSqlExecuteService.executeCustomSqlUpdate(sql, indexId, indexParams, batchSize);
```

### 2. 添加异常处理
```java
try {
    int affectedRows = customSqlExecuteService.executeCustomSqlUpdate(sql);
    // ... 处理逻辑
} catch (Exception e) {
    logger.error("分批删除失败，已删除 " + deletedCount + " 条记录", e);
    throw e; // 或根据业务需求决定是否继续
}
```

### 3. 添加日志
```java
logger.info("开始分批删除，总记录数：{}，批次大小：{}", count, batchSize);
logger.info("第{}批删除完成，删除{}条记录，累计删除{}条", batchNumber, affectedRows, deletedCount);
logger.info("分批删除完成，共删除{}条记录", deletedCount);
```

## 使用示例

将原来的代码：
```java
String originSqlCount = "select count(*) as count from " + table + " where index_id = " + indexId + " and index_params = '" + indexParams + "'";
int count = getCountVal(originSqlCount);
if (count == 0) {
    return;
}
String sql = "delete from " + table + " where index_id = " + indexId + " and index_params = '" + indexParams + "'";
customSqlExecuteService.executeCustomSqlUpdate(sql);
```

替换为方案1的代码即可。
