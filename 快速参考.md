# FreeSWITCH DESTINATION_OUT_OF_ORDER 错误快速参考

## 问题概述

**错误**: `-ERR DESTINATION_OUT_OF_ORDER`  
**服务器**: `10-181-10-158`  
**场景**: 高并发呼叫时出现，另一台服务器正常

## 快速诊断步骤

### 1. 运行诊断脚本
```bash
./诊断脚本.sh
```

### 2. 检查网关状态
```bash
fs_cli -x "sofia status gateway gwopensips"
```

### 3. 检查错误日志
```bash
tail -f /var/log/freeswitch/freeswitch.log | grep -i "DESTINATION_OUT_OF_ORDER"
```

### 4. 检查连接数
```bash
netstat -an | grep ESTABLISHED | wc -l
```

## 常见原因和快速修复

### 原因1: 网关连接数耗尽
**检查**: `fs_cli -x "sofia status gateway gwopensips"`  
**修复**: 
- 增加网关最大连接数配置
- 优化连接池管理
- 实现连接复用

### 原因2: 负载不均衡
**检查**: 对比两台服务器的呼叫量  
**修复**: 
- 调整负载均衡策略
- 实现基于负载的动态路由

### 原因3: 网络问题
**检查**: `ping` 网关服务器，检查网络延迟  
**修复**: 
- 检查网络配置
- 检查防火墙规则
- 优化网络连接

### 原因4: FreeSWITCH 资源限制
**检查**: 系统资源使用情况（CPU、内存、文件描述符）  
**修复**: 
- 增加系统资源限制
- 优化 FreeSWITCH 配置
- 调整最大会话数

## 临时解决方案

### 方案1: 实现自动重试
在 `FreeSwitchClientService.backgroundJobResultReceived` 方法中：
- 检测 `DESTINATION_OUT_OF_ORDER` 错误
- 自动重试（最多3次）
- 重试时切换到备用网关

### 方案2: 降级处理
- 检测到错误时，降低呼叫速率
- 将呼叫路由到另一台正常服务器
- 记录错误并告警

### 方案3: 网关重注册
```bash
fs_cli -x "sofia profile external restart gwopensips"
```

## 长期解决方案

1. **连接池优化**: 实现高效的连接池管理
2. **负载均衡**: 基于服务器负载和健康状态的路由
3. **监控告警**: 实时监控错误率和资源使用
4. **容错机制**: 自动故障转移和恢复
5. **压力测试**: 定期测试，提前发现问题

## 代码修改建议

### 修改点1: FreeSwitchClientService
```java
// 在 backgroundJobResultReceived 方法中
if (eventBody.contains("DESTINATION_OUT_OF_ORDER")) {
    // 1. 记录错误
    // 2. 触发重试或降级
    // 3. 发送告警
}
```

### 修改点2: 添加重试机制
```java
@Retryable(value = {FreeSwitchException.class}, maxAttempts = 3)
public String originate(...) {
    // originate 逻辑
}
```

### 修改点3: 实现负载均衡
```java
// 根据服务器健康状态和负载选择服务器
FreeSwitchServer server = loadBalancer.selectHealthyServer();
```

## 监控指标

建议监控以下指标：
- `freeswitch.error.destination_out_of_order` - 错误计数
- `freeswitch.connection.active` - 活跃连接数
- `freeswitch.gateway.status` - 网关状态
- `freeswitch.server.load` - 服务器负载

## 相关文件

- `问题分析.md` - 详细问题分析
- `解决方案实现.md` - 具体代码实现
- `诊断脚本.sh` - 自动化诊断脚本
