# 分批删除快速参考

## 核心原理

**问题**：一次性删除大量数据 → 长时间持有锁 → 死锁

**解决**：分批删除 → 每批独立事务 → 短暂延迟 → 释放锁

## 最简单的实现（Java）

```java
public int deleteInBatches(Long indexId, String indexParams) {
    int totalDeleted = 0;
    int deletedCount;
    
    do {
        // 每次删除1000条
        deletedCount = jdbcTemplate.update(
            "DELETE FROM ks_index_user_performance_details " +
            "WHERE index_id = ? AND index_params = ? LIMIT 1000",
            indexId, indexParams
        );
        
        totalDeleted += deletedCount;
        
        // 延迟50ms，释放锁
        if (deletedCount > 0) {
            Thread.sleep(50);
        }
    } while (deletedCount > 0);
    
    return totalDeleted;
}
```

## 最简单的实现（Python）

```python
def delete_in_batches(index_id, index_params, batch_size=1000):
    cursor = connection.cursor()
    total_deleted = 0
    
    while True:
        cursor.execute(
            """
            DELETE FROM ks_index_user_performance_details 
            WHERE index_id = %s AND index_params = %s LIMIT %s
            """,
            (index_id, index_params, batch_size)
        )
        
        deleted_count = cursor.rowcount
        total_deleted += deleted_count
        connection.commit()
        
        if deleted_count == 0:
            break
        
        time.sleep(0.05)  # 50ms延迟
    
    return total_deleted
```

## 最简单的实现（SQL存储过程）

```sql
DELIMITER $$
CREATE PROCEDURE delete_in_batches(
    IN p_index_id BIGINT,
    IN p_index_params VARCHAR(255),
    IN p_batch_size INT
)
BEGIN
    DECLARE v_deleted_count INT DEFAULT 1;
    
    WHILE v_deleted_count > 0 DO
        DELETE FROM ks_index_user_performance_details
        WHERE index_id = p_index_id 
          AND index_params = p_index_params
        LIMIT p_batch_size;
        
        SET v_deleted_count = ROW_COUNT();
        SELECT SLEEP(0.05);  -- 50ms延迟
    END WHILE;
END$$
DELIMITER ;

-- 调用
CALL delete_in_batches(123, 'param_value', 1000);
```

## 关键参数

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| **批次大小** | 1000 | 数据量小可增大，数据量大需减小 |
| **延迟时间** | 50ms | 高并发场景可增大到100-200ms |
| **事务方式** | REQUIRES_NEW | 每批使用独立事务，自动提交 |

## 批次大小选择

```
数据量 < 1万    → 批次大小 5000
数据量 1-10万   → 批次大小 1000  ⭐ 推荐
数据量 10-100万 → 批次大小 500
数据量 > 100万  → 批次大小 200
```

## 必须做的优化

### 1. 创建索引（最重要！）

```sql
CREATE INDEX idx_index_id_params 
ON ks_index_user_performance_details(index_id, index_params);
```

### 2. 使用独立事务（Java）

```java
@Transactional(propagation = Propagation.REQUIRES_NEW)
private int deleteOneBatch(...) {
    // 每批都是新事务，自动提交
}
```

### 3. 添加延迟

```java
Thread.sleep(50);  // Java
time.sleep(0.05)   // Python
SELECT SLEEP(0.05) -- SQL
```

## 完整示例对比

| 方式 | 代码复杂度 | 性能 | 适用场景 |
|------|-----------|------|----------|
| **LIMIT分批** | ⭐ 简单 | ⭐⭐⭐ 高 | **推荐，大多数场景** |
| 先查ID再删 | ⭐⭐ 中等 | ⭐⭐ 中 | 需要记录删除数据 |
| 存储过程 | ⭐ 简单 | ⭐⭐⭐ 高 | 减少网络往返 |

## 常见问题

**Q: 为什么需要延迟？**  
A: 给其他事务机会获取锁，降低死锁概率。

**Q: 批次大小如何选择？**  
A: 从1000开始，根据实际情况调整。数据量大就减小，数据量小就增大。

**Q: 需要手动提交事务吗？**  
A: 如果使用 `REQUIRES_NEW`，每批会自动提交。如果使用普通事务，需要手动提交。

**Q: 删除过程中可以中断吗？**  
A: 可以，每批都是独立的，中断后可以从头开始或继续。

## 检查清单

- [ ] 已创建联合索引 `(index_id, index_params)`
- [ ] 使用 LIMIT 限制每批删除数量
- [ ] 每批使用独立事务（REQUIRES_NEW）
- [ ] 每批删除后添加延迟
- [ ] 在低峰期执行大批量删除
- [ ] 监控删除进度和性能

## 相关文件

- 📖 **分批删除实现指南.md** - 详细实现说明
- 💻 **使用示例.md** - 完整代码示例
- 🔧 **分批删除完整示例.java** - Java完整实现
