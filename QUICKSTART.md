# ?? ??????

5??????Freeswitch???????

## ????

??????
- ? Python 3.7+
- ? Freeswitch 1.10+
- ? Redis
- ? MongoDB

## ??????????

### 1. ????

```bash
git clone <repository_url>
cd freeswitch-realtime-transcription
```

### 2. ????

```bash
# ??????
cp .env.example .env

# ???????mock ASR?????
vim .env
```

?????
```bash
ASR_PROVIDER=mock
FREESWITCH_HOST=127.0.0.1
```

### 3. ????

```bash
chmod +x start.sh
./start.sh
```

?????????????????

### 4. ????

????????
```
http://localhost:8080
```

## ????

### ??1: ???????Freeswitch?

????mock ASR??????????????

```bash
# ?????????Python??????
python3

# ????????????
import asyncio
import websockets
import json

async def test():
    uri = "ws://localhost:8765"
    async with websockets.connect(uri) as ws:
        # ????????
        await ws.send(json.dumps({
            "type": "session_start",
            "session_id": "test-session-001",
            "call_info": {
                "caller_number": "1000",
                "callee_number": "13800138000"
            }
        }))
        
        # ????????
        for i in range(5):
            # ????
            session_id_bytes = b"test-session-001"
            session_id_len = len(session_id_bytes).to_bytes(4, 'big')
            packet = session_id_len + session_id_bytes + b'\x00' + (b'\x00' * 3200)
            await ws.send(packet)
            await asyncio.sleep(1)
            
            # ????
            packet = session_id_len + session_id_bytes + b'\x01' + (b'\x00' * 3200)
            await ws.send(packet)
            await asyncio.sleep(1)

asyncio.run(test())
```

??Web????????????

### ??2: ?????????Freeswitch?

#### ??1: ??Freeswitch

```bash
# ??????
sudo cp freeswitch-config/dialplan/transcription.xml /etc/freeswitch/dialplan/default/
sudo cp freeswitch-config/scripts/transcription_stream.lua /usr/share/freeswitch/scripts/

# ??????
fs_cli -x "reloadxml"
```

#### ??2: ??????

??API?
```bash
curl -X POST http://localhost:5000/api/call/originate \
  -H "Content-Type: application/json" \
  -d '{
    "destination": "1000",
    "caller_id": "9999"
  }'
```

???fs_cli?
```bash
fs_cli
> originate user/1000 &park
```

#### ??3: ????

??Web???????????????????

## ????

### Q1: ??????

?? `.env` ???????
```bash
WS_PORT=9765  # ??????
API_PORT=5001
```

### Q2: ????WebSocket?

??????
```bash
sudo firewall-cmd --add-port=8765/tcp --permanent
sudo firewall-cmd --add-port=8766/tcp --permanent
sudo firewall-cmd --reload
```

### Q3: ASR?????

??????mock????
```bash
ASR_PROVIDER=mock
```

?????????ASR???

### Q4: Web?????

???????????WebSocket?????

## ???

- ?? ???? [README.md](README.md)
- ??? ?? [????](docs/ARCHITECTURE.md)
- ?? ?? [????](docs/DEPLOYMENT.md)
- ?? ?? [API??](docs/API.md)

## ??????

?????

## ????

- ?? [??Issue](https://github.com/your-repo/issues)
- ?? [???](https://github.com/your-repo/discussions)
- ?? ????

---

**??????????** ?
