# FreeSWITCH DESTINATION_OUT_OF_ORDER 错误快速排查指南

## 问题概述

**错误**: `-ERR DESTINATION_OUT_OF_ORDER`  
**服务器**: 10-181-10-158  
**场景**: 呼叫量大时出现，另一台服务器正常

## 快速排查步骤（按优先级）

### 🔴 第一步：立即检查（5分钟）

```bash
# 1. 检查文件描述符使用情况（最常见原因）
./diagnose_freeswitch.sh | grep -A 5 "文件描述符"

# 2. 检查网关状态
fs_cli -x "sofia status gateway gwopensips"

# 3. 检查当前会话数
fs_cli -x "show calls count"
```

**如果文件描述符使用率 > 80%**，这是最可能的原因！

### 🟡 第二步：对比两台服务器（10分钟）

```bash
# 在异常服务器上运行
./diagnose_freeswitch.sh > abnormal_server.txt

# 在正常服务器上运行
./diagnose_freeswitch.sh > normal_server.txt

# 对比关键指标
diff abnormal_server.txt normal_server.txt
```

### 🟢 第三步：实时监控（持续）

```bash
# 实时监控资源使用情况
./monitor_resources.sh 5
```

## 常见原因和解决方案

### 原因 1: 文件描述符耗尽 ⭐⭐⭐⭐⭐

**症状**: 
- 文件描述符使用率 > 90%
- 错误发生在高并发时

**解决方案**:
```bash
# 1. 临时增加限制
ulimit -n 65535

# 2. 永久修改（需要 root）
# 编辑 /etc/security/limits.conf
echo "freeswitch soft nofile 65535" >> /etc/security/limits.conf
echo "freeswitch hard nofile 65535" >> /etc/security/limits.conf

# 3. 修改 systemd 服务（如果使用 systemd）
# 编辑 /etc/systemd/system/freeswitch.service
# 添加: LimitNOFILE=65535
# 然后: systemctl daemon-reload && systemctl restart freeswitch
```

### 原因 2: SIP 网关连接数限制 ⭐⭐⭐⭐

**症状**:
- 网关状态显示连接数达到上限
- 网关在高负载时掉线

**解决方案**:
```bash
# 1. 检查网关配置
cat /usr/local/freeswitch/conf/sip_profiles/external/gwopensips.xml

# 2. 检查网关状态
fs_cli -x "sofia status gateway gwopensips"

# 3. 如果网关掉线，重新注册
fs_cli -x "sofia profile external rescan reloadxml"
fs_cli -x "sofia profile external register gwopensips"
```

### 原因 3: FreeSWITCH 并发限制 ⭐⭐⭐

**症状**:
- 会话数达到配置的最大值
- 新呼叫无法建立

**解决方案**:
```bash
# 1. 检查配置
grep -E "max-sessions|sessions-per-second" /usr/local/freeswitch/conf/autoload_configs/switch.conf.xml

# 2. 修改配置（根据实际情况调整）
# 编辑 switch.conf.xml，增加:
# <param name="max-sessions" value="10000"/>
# <param name="sessions-per-second" value="100"/>

# 3. 重新加载配置
fs_cli -x "reloadxml"
```

### 原因 4: 内存不足 ⭐⭐

**症状**:
- 内存使用率 > 90%
- 系统开始使用 swap

**解决方案**:
```bash
# 1. 检查内存
free -h

# 2. 检查 FreeSWITCH 内存泄漏
# 监控内存使用趋势

# 3. 优化配置或增加内存
```

### 原因 5: 网络问题 ⭐⭐

**症状**:
- 到网关的网络延迟高
- 网络丢包

**解决方案**:
```bash
# 1. 检查网络连接
ping -c 10 <gateway_ip>
mtr <gateway_ip>

# 2. 检查防火墙规则
iptables -L -n

# 3. 检查系统连接数限制
sysctl net.core.somaxconn
```

## 诊断脚本使用说明

### diagnose_freeswitch.sh
全面诊断脚本，收集所有相关信息：
```bash
./diagnose_freeswitch.sh
```

### compare_servers.sh
对比两台服务器的配置和状态：
```bash
./compare_servers.sh 10-181-10-158 normal-server
```

### monitor_resources.sh
实时监控资源使用情况：
```bash
# 每 5 秒更新一次
./monitor_resources.sh 5

# 每 1 秒更新一次（高负载时）
./monitor_resources.sh 1
```

## 日志分析

### 查找相关错误
```bash
# 查找 DESTINATION_OUT_OF_ORDER 错误
grep -i "DESTINATION_OUT_OF_ORDER" /usr/local/freeswitch/log/freeswitch.log

# 查找错误发生的时间模式
grep -i "DESTINATION_OUT_OF_ORDER" /usr/local/freeswitch/log/freeswitch.log | \
  awk '{print $1, $2}' | uniq -c | sort -rn

# 查找错误前后的上下文
grep -B 5 -A 5 "DESTINATION_OUT_OF_ORDER" /usr/local/freeswitch/log/freeswitch.log | tail -50
```

### 分析呼叫模式
```bash
# 统计呼叫量
grep "BACKGROUND_JOB" /usr/local/freeswitch/log/freeswitch.log | \
  grep "originate" | wc -l

# 按时间段统计
grep "BACKGROUND_JOB" /usr/local/freeswitch/log/freeswitch.log | \
  grep "originate" | awk '{print $1, $2}' | cut -d: -f1 | uniq -c
```

## 应急处理

如果问题正在发生：

1. **立即分流**
   ```bash
   # 将部分呼叫路由到正常服务器
   # 修改负载均衡配置或呼叫路由规则
   ```

2. **重启网关连接**
   ```bash
   fs_cli -x "sofia profile external killgw gwopensips"
   fs_cli -x "sofia profile external register gwopensips"
   ```

3. **临时增加限制**
   ```bash
   ulimit -n 65535
   # 重启 FreeSWITCH（如果可能）
   ```

4. **监控资源**
   ```bash
   ./monitor_resources.sh 1
   ```

## 预防措施

1. **设置监控告警**
   - 文件描述符使用率 > 80%
   - 内存使用率 > 85%
   - CPU 使用率 > 90%
   - 网关连接数 > 80%

2. **定期检查**
   - 每周运行诊断脚本
   - 对比两台服务器状态
   - 检查日志中的错误模式

3. **优化配置**
   - 根据实际负载调整并发限制
   - 优化 Lua 脚本性能
   - 合理设置超时时间

4. **负载均衡**
   - 确保两台服务器负载均衡
   - 实现自动故障转移
   - 监控呼叫分布

## 需要进一步帮助？

如果以上步骤无法解决问题，请收集以下信息：

1. 两台服务器的诊断报告（运行 `./diagnose_freeswitch.sh`）
2. 异常时间段的完整日志
3. 两台服务器的配置文件对比
4. 网络拓扑和网关配置信息
