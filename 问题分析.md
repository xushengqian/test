# FreeSWITCH 服务器异常问题分析

## 问题描述

**异常服务器**: 10-181-10-158  
**错误信息**: `-ERR DESTINATION_OUT_OF_ORDER`  
**触发条件**: 呼叫量大时  
**对比**: 另一台服务器正常

## 错误日志分析

```
Event-Name: BACKGROUND_JOB
Job-Command: originate
Job-Command-Arg: {session_type=1,object_type=2,agentid='40000260',originate_timeout=60,origination_caller_id_number='88888888',uuid='yryx_yrcall_1991323496101429248',mobile_number=13975850871,call_number=gw155555555}sofia/gateway/gwopensips/40000260 &lua(callout_ai.lua)
eventBody: ["-ERR DESTINATION_OUT_OF_ORDER"]
```

## 可能原因分析

### 1. 资源耗尽问题 ⚠️ 高优先级

#### 1.1 文件描述符限制
- **症状**: 高并发时无法创建新的连接
- **检查方法**: 
  ```bash
  ulimit -n
  lsof -p <freeswitch_pid> | wc -l
  cat /proc/<freeswitch_pid>/limits
  ```

#### 1.2 内存不足
- **症状**: 系统内存或 FreeSWITCH 进程内存耗尽
- **检查方法**:
  ```bash
  free -h
  ps aux | grep freeswitch
  ```

#### 1.3 CPU 过载
- **症状**: CPU 使用率持续 100%
- **检查方法**:
  ```bash
  top -p <freeswitch_pid>
  htop
  ```

### 2. 网关连接问题 ⚠️ 高优先级

#### 2.1 SIP 网关 (gwopensips) 连接数限制
- **症状**: 网关无法接受更多连接
- **检查方法**:
  ```bash
  fs_cli -x "sofia status gateway gwopensips"
  fs_cli -x "show calls"
  ```

#### 2.2 网关注册状态异常
- **症状**: 网关在高负载时掉线或注册失败
- **检查方法**:
  ```bash
  fs_cli -x "sofia status"
  fs_cli -x "sofia status profile internal"
  ```

### 3. FreeSWITCH 配置问题

#### 3.1 并发限制设置过低
- **检查位置**: `autoload_configs/switch.conf.xml`
- **关键参数**:
  - `max-sessions`
  - `sessions-per-second`
  - `max-db-handles`

#### 3.2 超时设置不合理
- **检查位置**: `autoload_configs/switch.conf.xml`
- **关键参数**:
  - `session-timeout`
  - `call-timeout`

### 4. 网络问题

#### 4.1 网络延迟/丢包
- **检查方法**:
  ```bash
  ping 10-181-10-158
  mtr 10-181-10-158
  ```

#### 4.2 防火墙/连接限制
- **检查方法**: 查看 iptables 规则和连接数限制

### 5. 系统级限制

#### 5.1 系统连接数限制
- **检查方法**:
  ```bash
  sysctl net.core.somaxconn
  sysctl net.ipv4.tcp_max_syn_backlog
  cat /proc/sys/net/core/somaxconn
  ```

#### 5.2 进程数限制
- **检查方法**:
  ```bash
  ulimit -u
  cat /proc/<freeswitch_pid>/limits
  ```

## 排查步骤

### 第一步：对比两台服务器配置
1. 对比 FreeSWITCH 配置文件
2. 对比系统资源限制
3. 对比网关配置

### 第二步：监控异常服务器
1. 实时监控资源使用情况
2. 监控网关状态
3. 监控错误日志

### 第三步：检查日志
1. FreeSWITCH 日志: `/usr/local/freeswitch/log/freeswitch.log`
2. 系统日志: `/var/log/messages` 或 `/var/log/syslog`
3. 网关日志: 检查 SIP 网关相关日志

## 建议的解决方案

### 短期方案
1. **增加资源限制**
   - 增加文件描述符限制
   - 增加系统连接数限制
   - 优化内存使用

2. **检查网关配置**
   - 确认网关连接数限制
   - 检查网关注册状态
   - 验证网关超时设置

3. **负载均衡**
   - 将部分呼叫分流到正常服务器
   - 检查负载均衡策略

### 长期方案
1. **性能优化**
   - 优化 FreeSWITCH 配置
   - 调整并发参数
   - 优化 Lua 脚本性能

2. **监控告警**
   - 建立资源监控
   - 设置告警阈值
   - 自动化故障恢复

3. **架构优化**
   - 考虑增加服务器
   - 优化呼叫路由策略
   - 实现自动故障转移

## 需要收集的信息

1. **系统信息**
   - 操作系统版本
   - 硬件配置（CPU、内存）
   - 系统资源限制

2. **FreeSWITCH 信息**
   - FreeSWITCH 版本
   - 配置文件
   - 日志文件（异常时间段）

3. **网络信息**
   - 网关配置
   - 网络拓扑
   - 防火墙规则

4. **对比信息**
   - 正常服务器的配置
   - 正常服务器的资源使用情况
