# FreeSWITCH 通道残留问题分析

## 问题描述
电话已挂断，但是 `show channels` 命令显示会话一直存在，通道没有被正确清理。

## 可能的原因

### 1. 通道状态机卡住
- 通道可能卡在 `CS_EXECUTE`、`CS_EXCHANGE_MEDIA` 或其他中间状态
- 挂断事件没有正确触发状态转换

### 2. 事件监听器问题
- CHANNEL_HANGUP 事件没有被正确处理
- 事件订阅者阻塞或异常，导致通道无法销毁

### 3. 应用程序层问题
- Lua/Python/其他脚本没有正确调用 `session:hangup()` 或 `session:destroy()`
- 应用程序持有通道引用，阻止垃圾回收

### 4. 媒体流问题
- RTP 流没有正确关闭
- 媒体服务器资源未释放

### 5. 桥接问题
- 桥接操作（bridge）没有正确结束
- 桥接的通道没有正确清理

### 6. 定时器或任务未清理
- 通道上的定时器（timer）仍在运行
- 后台任务没有正确取消

## 诊断步骤

### 1. 检查通道详细信息
```bash
# 在 fs_cli 中执行
show channels as json
show channels <uuid>
```

### 2. 检查通道状态
```bash
# 查看通道状态
show channels | grep -i state
```

### 3. 检查日志
```bash
# 查看 FreeSWITCH 日志
tail -f /usr/local/freeswitch/log/freeswitch.log | grep -i hangup
tail -f /usr/local/freeswitch/log/freeswitch.log | grep <channel-uuid>
```

### 4. 检查事件
```bash
# 在 fs_cli 中启用事件监听
/event plain CHANNEL_HANGUP CHANNEL_DESTROY
```

## 解决方案

### 方案 1: 强制销毁通道
```bash
# 在 fs_cli 中执行
uuid_kill <channel-uuid>
# 或
uuid_break <channel-uuid> all
```

### 2. 检查并修复应用程序代码
- 确保所有挂断路径都调用了 `session:hangup()` 或 `session:destroy()`
- 检查异常处理，确保即使出错也能清理通道
- 移除不必要的通道引用

### 3. 配置通道超时
在 `vars.xml` 或拨号计划中设置：
```xml
<variable name="call_timeout" value="3600"/>
<variable name="hangup_after_bridge" value="true"/>
```

### 4. 启用通道清理定时器
在 `autoload_configs/switch.conf.xml` 中：
```xml
<param name="max-sessions" value="1000"/>
<param name="sessions-per-second" value="30"/>
```

### 5. 监控和自动清理
- 实现监控脚本，定期检查并清理僵尸通道
- 设置通道最大存活时间

## 预防措施

1. **代码审查**
   - 确保所有挂断逻辑都正确实现
   - 使用 try-finally 确保资源清理

2. **日志监控**
   - 监控 CHANNEL_HANGUP 事件
   - 设置告警，当通道数量异常时通知

3. **压力测试**
   - 测试各种异常场景
   - 验证通道清理逻辑

4. **配置优化**
   - 设置合理的超时时间
   - 限制最大并发通道数
